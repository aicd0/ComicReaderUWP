<UserControl
    x:Class="ComicReaderUWP.UserControls.Reader.ReaderFrame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid
        x:Name="MainFrame"
        Padding="{x:Bind ViewModel.FrameMargin, Mode=OneWay}"
        Loaded="OnFrameLoaded"
        SizeChanged="OnFrameSizeChanged">
        <Image
            x:Name="ImageMerged"
            Width="{x:Bind ViewModel.FrameWidth, Mode=OneWay}"
            Height="{x:Bind ViewModel.FrameHeight, Mode=OneWay}"
            VerticalAlignment="Center"
            Source="{x:Bind ViewModel.ImageMerged, Mode=OneWay}" />
    </Grid>

</UserControl>
