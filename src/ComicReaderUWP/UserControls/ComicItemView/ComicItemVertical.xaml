<base:BaseUserControl
    x:Class="ComicReaderUWP.UserControls.ComicItemView.ComicItemVertical"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:ComicReaderUWP.Common.BaseUI"
    xmlns:controls="using:ComicReaderUWP.UserControls.Misc"
    xmlns:converters="using:ComicReaderUWP.Converters"
    xmlns:cw="using:CommunityToolkit.WinUI"
    xmlns:cwm="using:CommunityToolkit.WinUI.Media"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:image="using:ComicReaderUWP.Common.Imaging"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ComicReaderUWP.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:ComicItemViewModel}"
    Tapped="UserControl_Tapped"
    mc:Ignorable="d">

    <base:BaseUserControl.Resources>
        <ResourceDictionary>

            <converters:BooleanNegationConverter x:Key="BooleanNegationConverter" />
            <converters:BooleanToVisibilityNegationConverter x:Key="BooleanToVisibilityNegationConverter" />

        </ResourceDictionary>
    </base:BaseUserControl.Resources>

    <Grid
        x:Name="RootGrid"
        Background="{ThemeResource ButtonBackground}"
        ContextRequested="RootGrid_ContextRequested"
        PointerEntered="RootGrid_PointerEntered"
        PointerExited="RootGrid_PointerExited"
        Tapped="RootGrid_Tapped">
        <Grid.BackgroundTransition>
            <BrushTransition Duration="0:0:0.083" />
        </Grid.BackgroundTransition>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" MinHeight="100" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Padding="20,32,20,0">
                <Grid Height="{StaticResource ComicItemVerticalImageHeight}">
                    <Border
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        CornerRadius="5">
                        <image:SimpleImageView x:Name="ImageHolder" Uri="{x:Bind Item.CoverImageUri, Mode=OneWay}" />
                    </Border>
                </Grid>
            </Grid>

            <StackPanel
                Grid.Row="2"
                Margin="10,0,10,14"
                VerticalAlignment="Center"
                Orientation="Vertical"
                Spacing="3">
                <StackPanel
                    HorizontalAlignment="Left"
                    Opacity="0.6"
                    Orientation="Horizontal"
                    Spacing="4"
                    Visibility="{x:Bind Item.IsCollection, Mode=OneWay}">
                    <FontIcon
                        Margin="0,1,0,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Glyph="&#xF5ED;" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Text="{x:Bind StringResource.Collection}" />
                </StackPanel>
                <TextBlock
                    Margin="0,0,0,4"
                    FontSize="14"
                    FontWeight="SemiBold"
                    MaxLines="3"
                    Text="{x:Bind Item.Title, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="Wrap"
                    ToolTipService.ToolTip="{x:Bind Item.Title, Mode=OneWay}" />
                <StackPanel
                    HorizontalAlignment="Left"
                    Opacity="0.6"
                    Orientation="Horizontal"
                    Visibility="{x:Bind Item.IsRatingVisible, Mode=OneWay}">
                    <TextBlock
                        FontSize="12"
                        FontWeight="SemiBold"
                        Text="{x:Bind Item.Rating, Mode=OneWay}" />
                    <FontIcon
                        Margin="4,1,0,0"
                        FontSize="9"
                        Glyph="&#xE735;" />
                </StackPanel>
                <TextBlock
                    FontSize="12"
                    FontWeight="SemiLight"
                    Opacity="0.6"
                    Text="{x:Bind Item.PageCountAndProgress, Mode=OneWay}" />
            </StackPanel>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonBackground}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="PointerOver">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonBackgroundPointerOver}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>

</base:BaseUserControl>
