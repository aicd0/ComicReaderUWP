<UserControl
    x:Class="ComicReaderUWP.UserControls.Misc.EditLinksView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:ComicReaderUWP.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ComicReaderUWP.ViewModels"
    d:DesignHeight="100"
    d:DesignWidth="100"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>

            <converters:BooleanToVisibilityNegationConverter x:Key="BooleanToVisibilityNegationConverter" />

        </ResourceDictionary>
    </UserControl.Resources>

    <ListView
        Grid.Row="6"
        Grid.Column="2"
        ItemsSource="{x:Bind Links}"
        SelectionMode="None">
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>
        </ListView.ItemContainerStyle>
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="vm:LinkItemViewModel">
                <Grid>
                    <Grid Visibility="{x:Bind IsPlaceholder, Converter={StaticResource BooleanToVisibilityNegationConverter}, Mode=OneWay}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{x:Bind Name, Mode=TwoWay}" />
                        <TextBox
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            Text="{x:Bind Link, Mode=TwoWay}" />
                        <Button
                            Grid.Column="3"
                            Margin="8,0,0,0"
                            Padding="5,5,4,4"
                            Click="RemoveLinkButton_Click"
                            ToolTipService.ToolTip="{x:Bind StringResource.Remove}">
                            <Button.Content>
                                <FontIcon FontSize="16" Glyph="&#xE738;" />
                            </Button.Content>
                        </Button>
                    </Grid>
                    <StackPanel
                        MinHeight="36"
                        Orientation="Horizontal"
                        Visibility="{x:Bind IsPlaceholder, Mode=OneWay}">
                        <Button
                            Padding="5,5,4,4"
                            VerticalAlignment="Center"
                            Click="AddLinkButton_Click"
                            ToolTipService.ToolTip="{x:Bind StringResource.Add}">
                            <Button.Content>
                                <FontIcon FontSize="16" Glyph="&#xE710;" />
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</UserControl>
