<base:BaseUserControl
    x:Class="ComicReaderUWP.Views.Pages.Settings.AdvancedSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:ComicReaderUWP.Common.BaseUI"
    xmlns:converters="using:ComicReaderUWP.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:settings="using:ComicReaderUWP.Views.Pages.Settings"
    xmlns:vm="using:ComicReaderUWP.ViewModels"
    d:DesignHeight="100"
    d:DesignWidth="100"
    mc:Ignorable="d">

    <base:BaseUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/TextBlock.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:BooleanNegationConverter x:Key="BooleanNegationConverter" />
            <converters:BooleanToVisibilityNegationConverter x:Key="BooleanToVisibilityNegationConverter" />
        </ResourceDictionary>
    </base:BaseUserControl.Resources>

    <StackPanel Orientation="Vertical">
        <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
            <Button
                Click="OnClearCacheClick"
                Content="{x:Bind ViewModel.ClearCacheText, Mode=OneWay}"
                IsEnabled="{x:Bind ViewModel.IsClearingCache, Converter={StaticResource BooleanNegationConverter}, Mode=OneWay}" />
            <ProgressRing
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                IsActive="{x:Bind ViewModel.IsClearingCache, Mode=OneWay}" />
        </StackPanel>
        <Button
            Margin="0,8,0,0"
            Click="OnOpenUserDataFolderClick"
            Content="{x:Bind StringResource.OpenUserDataFolder}" />
        <Button
            Margin="0,8,0,0"
            Click="OnRefreshRandomSeedClick"
            Content="{x:Bind StringResource.RefreshRandomSeed}" />
        <TextBlock
            x:Uid="DebugMode"
            Margin="0,16,0,0"
            TextWrapping="Wrap" />
        <ToggleSwitch IsOn="{x:Bind ViewModel.Shared.DebugMode, Mode=OneWay}" Toggled="DebugModeToggleSwitch_Toggled" />
        <TextBlock
            Margin="0,8,0,0"
            Text="{x:Bind StringResource.SendUsageData}"
            TextWrapping="Wrap" />
        <ToggleSwitch IsOn="{x:Bind ViewModel.SendUsageData, Mode=TwoWay}" Toggled="SendUsageDataToggleSwitch_Toggled" />
        <Button
            Margin="0,32,0,0"
            Click="ResetAllSettingsButton_Click"
            Content="{x:Bind StringResource.ResetAllSettings}" />
    </StackPanel>

</base:BaseUserControl>
