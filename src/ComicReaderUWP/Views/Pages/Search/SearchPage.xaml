<base:BasePage
    x:Class="ComicReaderUWP.Views.Pages.Search.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:adaptivegrid="using:ComicReaderUWP.UserControls.AdaptiveGrid"
    xmlns:base="using:ComicReaderUWP.Common.BaseUI"
    xmlns:comicitemview="using:ComicReaderUWP.UserControls.ComicItemView"
    xmlns:converters="using:ComicReaderUWP.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:search="using:ComicReaderUWP.Views.Pages.Search"
    xmlns:vm="using:ComicReaderUWP.ViewModels"
    Background="Transparent"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/GridViewItem.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--  Value Converters  -->
            <converters:BooleanNegationConverter x:Key="BooleanNegationConverter" />
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <muxc:ProgressRing
            x:Name="SearchProgressRing"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="{x:Bind ViewModel.IsLoadingRingVisible, Mode=OneWay}"
            IsIndeterminate="True" />

        <!--  Search Results  -->
        <adaptivegrid:AdaptiveGridView
            x:Name="SearchResultGridView"
            Padding="15,15,15,0"
            ContainerContentChanging="OnGridViewContainerContentChanging"
            DesiredWidth="{StaticResource ComicItemHorizontalDesiredWidth}"
            ItemContainerStyle="{StaticResource SearchResultItemContainerExpandedStyle}"
            ItemHeight="{StaticResource ComicItemHorizontalDesiredHeight}"
            ItemsSource="{x:Bind ViewModel.SearchResults}"
            SelectionChanged="OnGridViewSelectionChanged"
            SelectionMode="{x:Bind ViewModel.ComicItemSelectionMode, Mode=OneWay}"
            StretchContentForSingleRow="False"
            Tapped="OnScrollViewerTapped">
            <adaptivegrid:AdaptiveGridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid
                        Margin="0,0,0,10"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </adaptivegrid:AdaptiveGridView.ItemsPanel>
            <adaptivegrid:AdaptiveGridView.ItemTemplate>
                <DataTemplate x:DataType="vm:ComicItemViewModel">
                    <comicitemview:ComicItemHorizontal />
                </DataTemplate>
            </adaptivegrid:AdaptiveGridView.ItemTemplate>
            <adaptivegrid:AdaptiveGridView.Header>
                <StackPanel Margin="15,0,0,16" Orientation="Vertical">
                    <TextBlock
                        FontSize="28"
                        FontWeight="SemiBold"
                        Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis" />
                    <TextBlock
                        Margin="0,2,0,0"
                        FontSize="14"
                        FontWeight="Normal"
                        Foreground="{ThemeResource ForegroundGrey2}"
                        Text="{x:Bind ViewModel.FilterDetails, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind ViewModel.FilterDetails, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}" />
                </StackPanel>
            </adaptivegrid:AdaptiveGridView.Header>
            <adaptivegrid:AdaptiveGridView.Footer>
                <Grid Height="60" Visibility="{x:Bind ViewModel.IsSelectMode, Mode=OneWay}" />
            </adaptivegrid:AdaptiveGridView.Footer>
        </adaptivegrid:AdaptiveGridView>

        <!--  No Results  -->
        <StackPanel
            Margin="10,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical"
            Visibility="{x:Bind ViewModel.IsNoResultTextVisible, Mode=OneWay}">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="28"
                FontWeight="SemiBold"
                Text="{x:Bind ViewModel.NoResultText, Mode=OneWay}"
                TextTrimming="CharacterEllipsis" />
            <TextBlock
                x:Uid="NoResultsDetail"
                Margin="0,15,0,0"
                HorizontalAlignment="Center"
                FontSize="18" />
        </StackPanel>

        <!--  Command Bar  -->
        <Grid
            VerticalAlignment="Bottom"
            Background="{ThemeResource CommandBarBackground}"
            Visibility="{x:Bind ViewModel.IsSelectMode, Mode=OneWay}">
            <StackPanel Margin="6,0" Orientation="Horizontal">
                <AppBarToggleButton
                    x:Uid="SelectAllAppBarButton"
                    Click="CommandBarSelectAllClicked"
                    Icon="SelectAll"
                    IsChecked="{x:Bind ViewModel.IsCommandBarSelectAllToggled, Mode=OneWay}" />
                <AppBarSeparator />
                <AppBarButton
                    Click="CommandBarFavoriteButton_Click"
                    Icon="Favorite"
                    IsEnabled="{x:Bind ViewModel.IsCommandBarFavoriteEnabled, Mode=OneWay}"
                    Label="{x:Bind StringResource.Favorite}" />
                <AppBarButton
                    Click="CommandBarUnfavoriteButton_Click"
                    Icon="UnFavorite"
                    IsEnabled="{x:Bind ViewModel.IsCommandBarUnFavoriteEnabled, Mode=OneWay}"
                    Label="{x:Bind StringResource.Unfavorite}" />
                <AppBarButton
                    Click="CommandBarCompletionStatusButton_Click"
                    Icon="Flag"
                    IsEnabled="{x:Bind ViewModel.IsAnyComicSelected, Mode=OneWay}"
                    Label="{x:Bind StringResource.CompletionStatus}" />
                <AppBarButton
                    Click="CommandBarHideButton_Click"
                    IsEnabled="{x:Bind ViewModel.IsCommandBarHideEnabled, Mode=OneWay}"
                    Label="{x:Bind StringResource.Hide}">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xED1A;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton
                    Click="CommandBarUnhideButton_Click"
                    IsEnabled="{x:Bind ViewModel.IsCommandBarUnHideEnabled, Mode=OneWay}"
                    Label="{x:Bind StringResource.Unhide}">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE7B3;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton
                    Click="CommandBarRemoveButton_Click"
                    IsEnabled="{x:Bind ViewModel.IsAnyComicSelected, Mode=OneWay}"
                    Label="{x:Bind StringResource.Remove}">
                    <AppBarButton.Icon>
                        <FontIcon Foreground="#FF4500" Glyph="&#xE74D;" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </StackPanel>
            <Rectangle
                Height="1"
                VerticalAlignment="Top"
                Fill="{ThemeResource CommandBarBorderBrush}" />
        </Grid>
    </Grid>
</base:BasePage>
