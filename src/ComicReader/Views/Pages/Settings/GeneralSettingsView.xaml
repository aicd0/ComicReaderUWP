<base:BaseUserControl
    x:Class="ComicReader.Views.Pages.Settings.GeneralSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:ComicReader.Common.BaseUI"
    xmlns:converters="using:ComicReader.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:settings="using:ComicReader.Views.Pages.Settings"
    xmlns:vm="using:ComicReader.ViewModels"
    d:DesignHeight="100"
    d:DesignWidth="100"
    mc:Ignorable="d">

    <base:BaseUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/TextBlock.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:BooleanToVisibilityNegationConverter x:Key="BooleanToVisibilityNegationConverter" />
        </ResourceDictionary>
    </base:BaseUserControl.Resources>

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource SettingsPageBodyTextBlock}"
                Text="{x:Bind StringResource.HomePageTapComicBehavior}" />
            <ComboBox
                Margin="12,0,0,0"
                DisplayMemberPath="Name"
                ItemsSource="{x:Bind ViewModel.HomePageTapComicBaheviors, Mode=OneWay}"
                SelectedIndex="{x:Bind ViewModel.HomePageTapComicBaheviorIndex, Mode=OneWay}"
                SelectionChanged="HomePageTapComicBehaviorComboBox_SelectionChanged" />
        </StackPanel>
    </StackPanel>

</base:BaseUserControl>
