<base:BaseUserControl
    x:Class="ComicReader.UserControls.Misc.SimpleTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:ComicReader.Common.BaseUI"
    xmlns:converters="using:ComicReader.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:vm="using:ComicReader.ViewModels"
    d:DesignHeight="100"
    d:DesignWidth="100"
    mc:Ignorable="d">

    <base:BaseUserControl.Resources>

        <converters:BooleanToVisibilityNegationConverter x:Key="BooleanToVisibilityNegationConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />

        <DataTemplate x:Key="NormalTemplate" x:DataType="vm:SimpleTreeViewNodeModel">
            <muxc:TreeViewItem
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                AllowDrop="False"
                AutomationProperties.Name="{x:Bind Title}"
                IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                ItemsSource="{x:Bind Children}"
                Tapped="TreeViewItem_Tapped">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="10" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" Height="18">
                        <Grid Visibility="{x:Bind CanExpand, Mode=OneWay}">
                            <FontIcon
                                Margin="0,2,0,0"
                                FontSize="14"
                                Glyph="{x:Bind Glyph, Mode=OneWay}"
                                Visibility="{x:Bind IsExpanded, Converter={StaticResource BooleanToVisibilityNegationConverter}, Mode=OneWay}" />
                            <FontIcon
                                Margin="0,2,0,0"
                                FontSize="14"
                                Glyph="{x:Bind Glyph, Mode=OneWay}"
                                Visibility="{x:Bind IsExpanded, Mode=OneWay}" />
                        </Grid>
                        <FontIcon
                            Margin="0,2,0,0"
                            FontSize="14"
                            Glyph="{x:Bind Glyph, Mode=OneWay}"
                            Visibility="{x:Bind CanExpand, Converter={StaticResource BooleanToVisibilityNegationConverter}, Mode=OneWay}" />
                    </Grid>
                    <TextBlock
                        Grid.Column="2"
                        VerticalAlignment="Center"
                        Text="{x:Bind Title, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis"
                        ToolTipService.ToolTip="{x:Bind Title, Mode=OneWay}" />
                    <TextBlock
                        Grid.Column="3"
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource ForegroundGrey2}"
                        Text="{x:Bind Description, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis"
                        Visibility="{x:Bind Description, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}" />
                </Grid>
            </muxc:TreeViewItem>
        </DataTemplate>

    </base:BaseUserControl.Resources>

    <muxc:TreeView
        x:Name="MainTreeView"
        CanDragItems="False"
        CanReorderItems="False"
        ContextRequested="TreeView_ContextRequested"
        ItemInvoked="TreeView_ItemInvoked"
        ItemTemplate="{StaticResource NormalTemplate}"
        ItemsSource="{x:Bind DataSource, Mode=OneWay}"
        Tapped="TreeView_Tapped" />

</base:BaseUserControl>
